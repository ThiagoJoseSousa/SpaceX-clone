Learned things: positioning, centering divs, displays, making scalable css, prioritize css over JS, 
css functions for responsiveness, css variables — despite not using it here, inset, margin, using translates,
background image, BEM, text responsiveness, and using intersection observer: the one watching intersections 
between user view and a box. 